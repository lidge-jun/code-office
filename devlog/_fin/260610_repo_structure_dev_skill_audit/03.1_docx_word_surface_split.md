# 03.1 DOCX Word Surface Split Patch Guide

Date: 2026-06-10
Target: `/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/Word.tsx`

## Why This Exists

`Word.tsx` is 979 lines and violates the dev skill's module-size rule. It is a coordinator, renderer, save bridge, SuperDoc adapter, dirty-state tracker, and DOCX XML repair engine in one file. This is the first structural split to implement when DOCX work resumes.

## Current Responsibility Map

| Current range | Responsibility | Future owner |
|---:|---|---|
| 1-35 | Imports, SuperDoc font asset imports, `Word.css` import | `Word.tsx`, `superdocFonts.ts` |
| 43-103 | DOCX constants, user, `SUPERDOC_FONT_ASSET_URLS`, SuperDoc modules, event names, save types | `docxConstants.ts`, `docxTypes.ts`, `superdocFonts.ts` |
| 105-129 | React refs/state setup | `Word.tsx` coordinator |
| 130-171 | `documentFile`, dirty setter, buffer update, `exportCurrentDocument` | `useDocxDocumentState.ts`, `docxExport.ts` |
| 173-215 | host-save request/waiter logic | `useDocxHostSave.ts` |
| 217-246 | toolbar save, wheel zoom, transaction/update dirty tracking | `useDocxDirtyState.ts`, `superdocZoom.ts` |
| 248-269 | `switchToViewer` save-before-view transition | `useDocxModeSwitch.ts` |
| 272-403 | WebView handler registration for open/save/host completion/init retry | `useDocxHostEvents.ts` |
| 405-435 | Cmd+S and cleanup/render timeout effects | `useDocxKeyboardSave.ts`, `useDocxRenderTimeout.ts` |
| 437-564 | JSX toolbar, warning, SuperDoc surface | `DocxModeToolbar.tsx`, `SuperDocSurface.tsx`, `Word.tsx` |
| 568-623 | timeout, zoom, name, error helpers | `docxRuntimeUtils.ts`, `superdocExceptions.ts` |
| 632-654 | visible text snapshot sanitization | `docxSnapshot.ts`, `docxText.ts` |
| 656-723 | SuperDoc export strategies and updated-doc map helpers | `docxExport.ts` |
| 725-787 | snippet detection and export validation | `docxSnapshot.ts`, `docxSaveValidation.ts`, `docxText.ts` |
| 789-969 | replacement-only DOCX XML repair | `docxSaveRepair.ts`, `docxText.ts` |

## New Files To Create

All files stay under the existing DOCX feature folder:

```text
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/docxConstants.ts
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/docxTypes.ts
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/superdocFonts.ts
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/superdocExceptions.ts
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/superdocZoom.ts
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/docxRuntimeUtils.ts
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/docxText.ts
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/docxSnapshot.ts
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/docxSaveValidation.ts (conditional; create only if `docxSnapshot.ts` would exceed 400 lines or validation needs a separate owner)
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/docxExport.ts
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/docxSaveRepair.ts
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/useDocxDocumentState.ts
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/useDocxDirtyState.ts
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/useDocxHostSave.ts
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/useDocxHostEvents.ts
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/useDocxModeSwitch.ts
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/useDocxKeyboardSave.ts
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/useDocxRenderTimeout.ts
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/DocxModeToolbar.tsx
/Users/jun/Developer/new/700_projects/code-office/src/react/view/word/SuperDocSurface.tsx
```

Do not create an `index.ts` barrel. `dev-architecture` bans internal barrels for feature folders because they hide dependencies and can create import cycles.

## Patch Sequence

### Patch 1 - Constants, Types, Fonts

Move from `Word.tsx`:

- `DOCX_MIME`
- `DOCX_EXPORT_TIMEOUT_MS`
- `DOCX_REPAIR_TIMEOUT_MS`
- `DOCX_RENDER_TIMEOUT_MS`
- `DOCX_HOST_SAVE_TIMEOUT_MS`
- `DOCX_USER`
- `DOCX_SUPERDOC_MODULES`
- `DOCX_EVENTS`
- `DocxSavePurpose`
- `HostSaveResult`
- `HostSaveWaiter`
- all SuperDoc font asset imports and `SUPERDOC_FONT_ASSET_URLS`

Expected imports in `Word.tsx` after patch:

```ts
import {
    DOCX_EVENTS,
    DOCX_EXPORT_TIMEOUT_MS,
    DOCX_HOST_SAVE_TIMEOUT_MS,
    DOCX_MIME,
    DOCX_RENDER_TIMEOUT_MS,
    DOCX_REPAIR_TIMEOUT_MS,
    DOCX_SUPERDOC_MODULES,
    DOCX_USER,
} from './docxConstants';
import type { DocxSavePurpose, HostSaveResult, HostSaveWaiter } from './docxTypes';
import { SUPERDOC_FONT_ASSET_URLS } from './superdocFonts';
```

Verification:

```text
npm run typecheck
npm run test:docx-editor-provider
```

### Patch 2 - SuperDoc Utility Extraction

Create `superdocZoom.ts`:

- move `applySuperDocZoom()`

Create `docxRuntimeUtils.ts`:

- move `withTimeout()`
- move `normalizeDocumentName()`
- move `stripDocxExtension()`

Create `superdocExceptions.ts`:

- move `formatUnknownError()`
- move `isFatalSuperDocException()`
- move `isIgnorableSuperDocException()`
- move `extractErrorMessage()`
- move `isSuperDocElementsError()`

Reason: these helpers are not React stateful and should not keep `Word.tsx` large.

Create `docxText.ts` as a leaf module:

- move `normalizeEditorText()`
- move `splitEditorTextLines()`
- move `extractDocxText()`
- move `decodeXmlText()`
- move `encodeXmlText()`

`docxText.ts` must not import `docxSnapshot.ts`, `docxSaveRepair.ts`, React, Ant Design, `handler`, or `@superdoc-dev/react`. Both `docxSnapshot.ts` and `docxSaveRepair.ts` import this leaf module to avoid a circular dependency.

### Patch 3 - Snapshot And Validation Extraction

Create `docxSnapshot.ts`:

- move `readEditorTextSnapshot()`
- move `sanitizeEditorSnapshotText()`
- move `getChangedTextSnippets()`
- move `getMissingVisibleTextSnippetsFromSource()`
- move `isRelevantVisibleLine()`
- move `mergeTextSnippets()`
- move `getRelevantTextTokens()`
- import text normalization/XML text helpers from `docxText.ts`

Create `docxSaveValidation.ts` only if keeping `assertDocxContainsTextSnippets()` in `docxSnapshot.ts` would push `docxSnapshot.ts` over 400 lines, or if validation becomes a distinct owner during extraction:

- move `assertDocxContainsTextSnippets()`

If `docxSnapshot.ts` would exceed 400 lines after tests are added, put validation into `docxSaveValidation.ts`; otherwise keep it in `docxSnapshot.ts` for cohesion.

### Patch 4 - Export And Repair Extraction

Create `docxExport.ts`:

- move `exportEditorDocx()`
- move `isUpdatedDocMap()`
- move `patchDocxParts()`

Create `docxSaveRepair.ts`:

- move `patchDocxTextFromSnapshots()`
- move `repairDocxTextFromSnapshots()`
- move `extractDocxParagraphTexts()`
- move `findBestSourceParagraph()`
- move `hasStrongParagraphTokenOverlap()`
- move `getParagraphOverlapScore()`
- move `getComparableParagraphTokens()`
- move `replaceParagraphText()`
- import XML text helpers from `docxText.ts`

Boundary rule:

- `docxSnapshot.ts` and conditional `docxSaveValidation.ts` may import `JSZip` only for reading `word/document.xml` during source/export validation.
- `docxExport.ts` may import `JSZip` only for `patchDocxParts()`.
- `docxSaveRepair.ts` may import `JSZip` and `DOCX_MIME`.
- `docxSaveRepair.ts` must not import React, Ant Design, `handler`, or `@superdoc-dev/react`.
- `docxExport.ts` may depend on SuperDoc-shaped structural types but should avoid importing React components.

### Patch 5 - Host-Save Hooks

Create `useDocxHostSave.ts`:

- owns `saveRequestTimerRef`
- owns `hostSaveInProgressRef`
- owns `hostSaveWaitersRef`
- exposes:

```ts
type UseDocxHostSaveOptions = {
    setDirty: (dirty: boolean) => void;
};

type UseDocxHostSaveResult = {
    hostSaveInProgressRef: React.MutableRefObject<boolean>;
    requestHostSave: () => void;
    requestHostSaveAndWait: () => Promise<void>;
    resolveHostSaveWaiters: (result: HostSaveResult) => void;
};
```

Keep validation at the WebView-host boundary. Do not add runtime validation around internal typed hook parameters.

Create `useDocxHostEvents.ts`:

- register `open`
- register `openBuffer`
- register `saveRequest`
- register `hostSaveCompleted`
- perform init retry

This hook will need many dependencies. If the dependency list becomes hard to audit, split save handling into `useDocxSaveRequest.ts`.

Stability invariant: `useDocxHostEvents.ts` must register WebView handlers and init retry from a stable effect. It must not depend directly on `documentBuffer`, `documentName`, `mode`, or `exportCurrentDocument`. Use refs such as `exportCurrentDocumentRef`, `latestSaveBufferRef`, `editorTextSnapshotRef`, and `lastPersistedTextSnapshotRef` so save handlers see current state without re-emitting `init` or replacing handlers during ordinary document updates.

### Patch 6 - Document State, Dirty State, Mode, Keyboard, Timeout Hooks

Create `useDocxDocumentState.ts`:

- own the current `documentFile` memoization/update path,
- own edit buffer replacement after `openBuffer`,
- expose the current file object and buffer update API needed by the coordinator.

Create `useDocxDirtyState.ts`:

- own dirty-state mutation helper,
- own transaction/update dirty tracking,
- receive `readEditorTextSnapshot()` and snapshot comparison helpers as dependencies or imports from `docxSnapshot.ts`.

Create `useDocxModeSwitch.ts`:

- move `switchToViewer`,
- perform save-before-view transition,
- keep mode change behavior equivalent to the current toolbar path.

Create `useDocxKeyboardSave.ts`:

- move Cmd+S registration and cleanup,
- call the same host-save request path used by the toolbar.

Create `useDocxRenderTimeout.ts`:

- move render timeout effect setup and cleanup,
- expose the warning/error state needed by `Word.tsx` and `SuperDocSurface.tsx`.

Patch 6 is mandatory, not optional. Without it, `Word.tsx` keeps too much state/effect logic and may miss the under-350-line target.

### Patch 7 - UI Components

Create `DocxModeToolbar.tsx`:

- title `DOCX`
- mode metadata
- View/Edit segmented control
- Save button in edit mode

Create `SuperDocSurface.tsx`:

- `<main className="docx-superdoc-container">`
- `SuperDocEditor` wiring
- warning/rendering status is still passed by props

Rule: keep `SuperDocSurface.tsx` purely presentational except for direct SuperDoc event pass-through props. Dirty/save mode decisions remain in hooks or `Word.tsx`.

SuperDoc invariants to preserve in `SuperDocSurface.tsx`:

- `key={`${documentName}:${documentVersion}`}` changes only when bytes/name version changes, not for View/Edit mode alone.
- `ref={superdocRef}` continues to expose the live SuperDoc instance to the coordinator.
- `role="editor"` stays stable.
- `hideToolbar={false}` stays stable; viewer toolbar hiding remains CSS-driven.
- `documentMode={mode === 'viewer' ? 'viewing' : 'editing'}` remains the View/Edit mapping.
- `allowSelectionInViewMode={true}` remains set.
- `modules={DOCX_SUPERDOC_MODULES}` remains stable and keeps tracked changes disabled.
- telemetry remains disabled.
- font resolution uses `SUPERDOC_FONT_ASSET_URLS`.
- `onReady` and `onEditorCreate` keep updating `bodyEditorRef`, applying zoom, clearing rendering/loading, refreshing editor text snapshot, and updating `lastPersistedTextSnapshotRef`.

## Expected Final Shape

`Word.tsx` should become a coordinator under 350 lines:

- state wiring,
- hook composition,
- small mode handlers,
- render composition.

No extracted file should exceed 400 lines.

## Tests To Update

Target:

```text
/Users/jun/Developer/new/700_projects/code-office/src/test/docxEditorProviderTest.mjs
```

Current tests grep `Word.tsx` for many helper functions. After extraction:

- change assertions to read all DOCX source files under `src/react/view/word/`;
- preserve behavior assertions, but allow functions to live in new helper files;
- add negative assertions:
  - `Word.tsx` does not import `JSZip`;
  - `Word.tsx` does not define `patchDocxTextFromSnapshots`;
  - `Word.tsx` does not define `repairDocxTextFromSnapshots`;
  - `docxSaveRepair.ts` does not import React or `handler`;
  - no `src/react/view/word/index.ts` barrel exists.

Suggested helper inside the test:

```js
const wordDir = path.join(root, 'src/react/view/word');
const docxSourceFiles = (await readdir(wordDir))
  .filter((file) => /\.(ts|tsx)$/.test(file))
  .filter((file) => file !== 'index.ts');
const docxSources = await Promise.all(
  docxSourceFiles.map(async file => [file, await readFile(path.join(wordDir, file), 'utf8')])
);
const combinedWordSource = docxSources.map(([, source]) => source).join('\n');
```

Preserve the existing assertion that `init` is not re-emitted when `exportCurrentDocument` changes. The assertion should check the extracted `useDocxHostEvents.ts` path and the coordinator ref path rather than assuming all logic remains in `Word.tsx`.

## Verification Gate

Run after each patch or small batch:

```text
npm run test:docx-editor-provider
npm run typecheck
npx --yes madge --circular --extensions ts,tsx src --exclude 'src/bundle|src/react/view/excel/x-spreadsheet|resource'
```

Run after the full split:

```text
npm run test:ci
npm run build
```

## Stop Conditions

Stop and re-plan before continuing if:

- `Word.tsx` still exceeds 400 lines after Patch 7.
- Any new circular dependency appears.
- Extracted helper files require imports back from `Word.tsx`.
- Save behavior tests must be weakened to pass.
