# 03.1 Phase 03 — Wikilink Implementation Plan (resolve parity + unresolved UX)

> Diff-level implementation plan for Phase 3. Spec: `03_phase_03_wikilink_webview_export.md`.
> Reverse-engineering survey: `92a_research_phase03_obsidian_webview_export.md`.
> Confirmed scope (2026-05-31, user): Q1=A export resolve parity, Q2=A unresolved
> styling + create-note, Q3=A section/blockId export + embed out-of-scope.
> Phase 3 ONLY. No other phases touched.

## Part 1 — Plain Explanation

Phase 3의 핵심 경로(미리보기 클릭, export 변환)는 이미 구현돼 있다. 이 단계는
**역공학 비교로 드러난 3개 gap을 마감**한다: (1) export가 미리보기와 똑같은
closest-note 리졸버를 쓰게 해서 클릭과 내보내기 결과를 일치시키고, (2) 없는 노트는
미리보기에서 흐리게(점선) 보이게 하고 클릭 시 "새 노트 만들까요?"를 묻고, (3)
`[[#Section]]`·`^blockId`의 export 동작을 정의하고 `![[embed]]`는 범위 밖으로 명문화한다.

## Part 2 — Touch Map

```
src/service/wikilink/wikilinkResolver.ts   MODIFY  resolveExportTarget(), noteBasenameIndex(), create-note on miss
src/service/markdownService.ts             MODIFY  inject resolver, build wikilink map before export
src/service/markdown/markdown-pdf.js       MODIFY  thread map, resolved href, anchor-slug, skip embeds
src/provider/markdownEditorProvider.ts     MODIFY  send wikilinkIndex in open payload (async)
src/extension.ts                           MODIFY  pass resolver into MarkdownService
resource/vditor/index.js                   MODIFY  forward md.wikilinkIndex to util
resource/vditor/util.js                    MODIFY  mark is-unresolved spans, setWikilinkIndex()
resource/vditor/index.css                  MODIFY  .vscode-obsdian-wikilink.is-unresolved style
```

Broad change: 8 files, host + webview. No new directories/modules.

---

## Q1 — Export resolve parity (closest-note reused)

### MODIFY `src/service/wikilink/wikilinkResolver.ts`

Add a **non-interactive** resolution method that reuses the existing
`findCandidates()` scoring (single source of scoring → true parity with the click
path). No QuickPick, no warnings; deterministic top candidate.

```ts
// after resolve(...) { ... }
async resolveExportTarget(
    sourceUri: vscode.Uri,
    link: ParsedWikilink
): Promise<{ href: string; resolved: boolean } | undefined> {
    const rawTarget = normalizeTarget(link.target.trim());
    if (!rawTarget) return undefined;                       // heading/block-only → caller emits same-doc anchor
    const workspaceFolder = vscode.workspace.getWorkspaceFolder(sourceUri);
    if (!workspaceFolder || isUnsafeTarget(rawTarget)) return { href: '', resolved: false };
    const sourceDir = path.dirname(sourceUri.fsPath);
    const candidates = await this.findCandidates(workspaceFolder, sourceDir, rawTarget);
    if (candidates.length === 0) return { href: '', resolved: false };
    const rel = normalizePath(path.relative(sourceDir, candidates[0].uri.fsPath));
    return { href: rel, resolved: true };                   // includes .md, relative to source doc
}

async noteBasenameIndex(): Promise<string[]> {
    const files = await this.listMarkdownFiles();
    return [...new Set(files.map(u => stripMarkdownExtension(path.basename(u.fsPath)).toLowerCase()))];
}
```

### Create-note on miss (Q2 click behavior — same file)

Replace the silent warning in `resolve()` with a modal create prompt.

```diff
         const candidates = await this.findCandidates(workspaceFolder, sourceDir, target);
         if (candidates.length === 0) {
-            vscode.window.showWarningMessage(`No Markdown note found for [[${link.target}]].`);
-            return undefined;
+            return this.offerCreateNote(sourceUri, workspaceFolder, target);
         }
```

```ts
private async offerCreateNote(
    sourceUri: vscode.Uri, workspaceFolder: vscode.WorkspaceFolder, target: string
): Promise<vscode.Uri | undefined> {
    const name = stripMarkdownExtension(path.basename(normalizePath(target)));
    if (!name) return undefined;
    const pick = await vscode.window.showInformationMessage(
        `Note "${name}" not found. Create it?`, { modal: true }, 'Create');
    if (pick !== 'Create') return undefined;
    const dest = path.resolve(path.dirname(sourceUri.fsPath),
        targetHasMarkdownExtension(target) ? target : `${target}.md`);
    if (!isInside(workspaceFolder.uri.fsPath, dest)) return undefined;
    const uri = vscode.Uri.file(dest);
    await vscode.workspace.fs.writeFile(uri, new TextEncoder().encode(`# ${name}\n`));
    return uri;
}
```

### MODIFY `src/extension.ts`

```diff
-	const markdownService = new MarkdownService(context);
 	const wikilinkResolver = new WikilinkResolver();
+	const markdownService = new MarkdownService(context, wikilinkResolver);
```
(`wikilinkResolver` is already declared on the next line today — reorder so it
precedes `markdownService`.)

### MODIFY `src/service/markdownService.ts`

```diff
-import { convertMd } from "./markdown/markdown-pdf";
+import { convertMd } from "./markdown/markdown-pdf";
+import { WikilinkResolver } from "@/service/wikilink/wikilinkResolver";
+import { findWikilinks } from "@/service/wikilink/wikilinkParser";
+import { readFileSync } from 'fs';
```
```diff
-    constructor(private context: vscode.ExtensionContext) {
-    }
+    constructor(private context: vscode.ExtensionContext, private wikilinkResolver?: WikilinkResolver) {
+    }
```
```diff
-            await convertMd({ markdownFilePath: uri.fsPath, config: this.getConfig(option) })
+            const wikilinkMap = await this.buildWikilinkMap(uri);
+            await convertMd({ markdownFilePath: uri.fsPath, config: this.getConfig(option), wikilinkMap })
```
```ts
private async buildWikilinkMap(uri: vscode.Uri): Promise<Record<string, { href: string; resolved: boolean }>> {
    const map: Record<string, { href: string; resolved: boolean }> = {};
    if (!this.wikilinkResolver) return map;
    const text = readFileSync(uri.fsPath, 'utf8');
    for (const link of findWikilinks(text)) {
        if (link.embed || map[link.raw] !== undefined) continue;   // ![[...]] out-of-scope
        const r = await this.wikilinkResolver.resolveExportTarget(uri, link);
        if (r) map[link.raw] = r;
    }
    return map;
}
```

### MODIFY `src/service/markdown/markdown-pdf.js`

Thread the map and emit resolved hrefs + anchor-compatible slugs; skip embeds.

```diff
-export const convertMd = async (options) => {
+export const convertMd = async (options) => {
   ...
-  const content = convertMarkdownToHtml(inputMarkdownFile, type, text, config)
+  const content = convertMarkdownToHtml(inputMarkdownFile, type, text, config, options.wikilinkMap || {})
```
```diff
-function convertMarkdownToHtml(filename, type, text, config) {
+function convertMarkdownToHtml(filename, type, text, config, wikilinkMap = {}) {
```
```diff
-      .use(markdownItWikilink)
+      .use(md2 => markdownItWikilink(md2, wikilinkMap))
```
```diff
 function markdownItWikilink(md) {
+function markdownItWikilink(md, wikilinkMap = {}) {
   md.inline.ruler.before('emphasis', 'code-office_wikilink', (state, silent) => {
     const start = state.pos;
+    // ![[embed]] is out-of-scope: leave untransformed
+    if (state.src.charCodeAt(start - 1) === 0x21) return false;
     if (state.src.charCodeAt(start) !== 0x5B || state.src.charCodeAt(start + 1) !== 0x5B) {
       return false;
     }
     const end = state.src.indexOf(']]', start + 2);
     if (end === -1) return false;
     if (!silent) {
       const body = state.src.slice(start + 2, end);
-      const parsed = parseWikilinkExportBody(body);
+      const parsed = parseWikilinkExportBody(body, wikilinkMap);
       if (!parsed) return false;
       const tokenOpen = state.push('link_open', 'a', 1);
-      tokenOpen.attrs = [['href', parsed.href], ['class', 'code-office-wikilink']];
+      tokenOpen.attrs = [['href', parsed.href],
+        ['class', parsed.resolved ? 'code-office-wikilink' : 'code-office-wikilink code-office-wikilink--unresolved']];
       ...
```

`parseWikilinkExportBody` — use the resolved relative href when available, support
heading-only same-doc links, and align heading fragments to `markdown-it-anchor`'s
default slug so `[[#Section]]` lands:

```js
function parseWikilinkExportBody(body, wikilinkMap = {}) {
  const trimmed = body.trim();
  if (!trimmed) return undefined;
  const [targetWithFrag, alias] = splitOnce(trimmed, '|');
  const [rawTarget, fragment] = splitOnce(targetWithFrag.trim(), '#');
  const cleanTarget = rawTarget.trim().replace(/\\/g, '/');
  if (cleanTarget && isUnsafeWikilinkTarget(rawTarget.trim())) return undefined;

  const frag = fragment ? buildFragment(fragment.trim()) : '';   // #slug or #blockid
  // heading/block-only → same-document anchor
  if (!cleanTarget) {
    if (!frag) return undefined;
    return { href: frag, label: alias?.trim() || fragment.trim(), resolved: true };
  }

  const mapped = wikilinkMap[body];
  const base = mapped && mapped.resolved
    ? mapped.href                                                // closest-note relative path (.md)
    : (cleanTarget.match(/\.(md|markdown)$/i) ? cleanTarget : `${cleanTarget}.md`);
  const label = alias?.trim() || fragment?.trim() || path.basename(cleanTarget);
  return { href: `${base}${frag}`, label, resolved: !mapped || mapped.resolved };
}

function buildFragment(fragment) {
  if (fragment.startsWith('^')) return `#${slugifyExport(fragment.slice(1))}`;   // block id
  const heading = splitOnce(fragment, '^')[0].trim();
  return heading ? `#${slugifyExport(heading)}` : '';
}

// markdown-it-anchor default slug
function slugifyExport(s) {
  return encodeURIComponent(String(s).trim().toLowerCase().replace(/\s+/g, '-'));
}
```

---

## Q2 — Unresolved styling (preview)

### MODIFY `src/provider/markdownEditorProvider.ts`

`buildOpenPayload` becomes async to include the note index; await it in `init`/`reload`.

```diff
-        const buildOpenPayload = () => ({
+        const buildOpenPayload = async () => ({
             title: basename(uri.fsPath),
             config,
             scrollTop: this.state.get(`scrollTop_${document.uri.fsPath}`, 0),
             language: vscode.env.language,
-            rootPath, content
+            rootPath, content,
+            wikilinkIndex: await this.wikilinkResolver?.noteBasenameIndex() ?? []
         });
```
```diff
-        handler.on("init", () => {
-            handler.emit("open", buildOpenPayload())
+        handler.on("init", async () => {
+            handler.emit("open", await buildOpenPayload())
             this.updateCount(content)
             this.countStatus.show()
```
```diff
-        }).on("reload", async () => {
+        }).on("reload", async () => {
             reloadFromDisk();
             await this.updateTextDocument(document, content);
-            handler.emit("open", buildOpenPayload())
+            handler.emit("open", await buildOpenPayload())
```

### MODIFY `resource/vditor/index.js`

```diff
 handler.on("open", async (md) => {
   const { config, language } = md;
+  setWikilinkIndex(md.wikilinkIndex || []);
```
```diff
-import { openLink, hotKeys, ..., installMarkdownPostProcessing } from "./util.js";
+import { openLink, hotKeys, ..., installMarkdownPostProcessing, setWikilinkIndex } from "./util.js";
```

### MODIFY `resource/vditor/util.js`

```diff
+let wikilinkIndex = new Set();
+export function setWikilinkIndex(list) { wikilinkIndex = new Set(list || []); }
+
+function isUnresolvedWikilink(body) {
+    const target = body.split('|')[0].split('#')[0].trim();
+    if (!target) return false;                 // same-doc heading/block link
+    const base = target.split(/[\\/]/).pop().replace(/\.(md|markdown)$/i, '').toLowerCase();
+    return !wikilinkIndex.has(base);
+}
```
```diff
     roots.forEach(root => replaceTextMarkers(root, /(!)?\[\[([^\]\r\n]+)\]\]/g, (match) => {
+        if (match[1]) return document.createTextNode(match[0]);   // ![[embed]] out-of-scope
         const span = document.createElement('span');
-        span.className = 'vscode-obsdian-wikilink';
+        span.className = isUnresolvedWikilink(match[2])
+            ? 'vscode-obsdian-wikilink is-unresolved' : 'vscode-obsdian-wikilink';
         span.setAttribute('data-wikilink', match[2]);
         span.textContent = displayWikilink(match[2]);
         return span;
     }));
```

### MODIFY `resource/vditor/index.css`

```css
.vscode-obsdian-wikilink.is-unresolved {
    color: var(--vscode-disabledForeground, #999);
    border-bottom: 1px dashed currentColor;
    opacity: 0.75;
}
```

---

## Q3 — Edge cases (decision record)

| Case | Export behavior |
|---|---|
| `[[Note]]` | closest-note relative path (`.md`), `code-office-wikilink` |
| `[[Note#Heading]]` | resolved path + `#<anchor-slug>` |
| `[[Note#^block]]` | resolved path + `#<blockid-slug>` |
| `[[#Heading]]` | same-document anchor `#<anchor-slug>` (no `.md`) |
| `[[#^block]]` | same-document anchor `#<blockid-slug>` |
| unresolved `[[Missing]]` | naive `Missing.md` + `code-office-wikilink--unresolved` |
| `![[embed]]` | **out-of-scope** — left untransformed in preview and export |

---

## Verification (B/C phase)

1. `npm run typecheck` (`tsc --noEmit` + react tsconfig) passes.
2. `npm run build` (vite) — `markdown-pdf.js` bundles with new `import`s.
3. Manual parity check: a `[[Note]]` in a subdir opens the same file on click and on
   export HTML; a missing note shows dashed style + create prompt.

## Business-logic risk to confirm (single open question)

- **Heading anchor slug**: `slugifyExport` assumes `markdown-it-anchor`'s default
  slugify (`lowercase`, spaces→`-`, `encodeURIComponent`). If a custom slugify is
  later configured on `markdownItAnchor`/`markdownItToc`, `[[#Section]]` fragments
  must be regenerated to match. Verify against rendered heading `id`s in B.

## Failed / 미검증
- markdown-it-anchor의 정확한 기본 slugify 동작은 빌드 후 실제 export HTML의
  heading `id`로 재확인 필요(현재 소스 미확인, 기본값 가정).
